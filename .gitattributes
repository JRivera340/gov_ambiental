# Los scripts que corren dentro de contenedores Linux necesitan salto de
# línea LF sin importar en qué sistema operativo se clonó el repo — un CRLF
# en el shebang rompe el script ("bad interpreter") dentro del contenedor.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
